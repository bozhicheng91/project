        该程序实现了波前重建算法。
        
        算法实现效果为鱼鳞式。
        
        首先需要确定初始点，初始点的k近邻点集为局部重建样本进行cocone
 重建。通过边界样点检测，确定边界点集。再对边界点集求取k近邻，获得局部
 点集的外围保护点。在对面片进行选择的时候，在对面片进行选择的时候，若
 面片的三个顶点都是保护点，则剔除该面片。
 
        输入点集设置在test_ring_extending_coco.cpp 文件中。
        输入参数在test_ring_extending_coco.h 文件中，其中INITIAL_SIZE
        设置的为初始重建网格网格的数据大小。EXTENDING_SIZE 设置的为波
        前扩展所需要的数据大小。BORDER_PRO_SIZE 设置的为边界保护点搜索
        数据的大小。
        
